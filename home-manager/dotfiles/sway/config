# Set mod key (Mod1=<Alt>, Mod4=<Super>)
set $mod Mod4

set $left h
set $down j
set $up k
set $right l

set $term kitty
set $menu rofi

### Output configuration
output * bg /home/philipp/Pictures/wallpaper.jpg fill

output eDP-1 {
    scale 1.5
}

### Input configuration
input type:keyboard {
    xkb_layout de
    xkb_variant deadgraveacute
    xkb_options caps:swapescape
}

input type:touchpad {
    tap enabled
    drag_lock disabled
    scroll_factor 1.0
}

seat * xcursor_theme capitaine-cursors 36


### Keybindings
#
# Basics:
#
    # Launch terminal
    bindsym $mod+Return exec $term

    # Kill focused window
    bindsym $mod+Shift+q kill

    # xkill-style: click on a window to kill it
    bindsym $mod+Shift+x exec --no-startup-id $HOME/bin/force-kill

    # Lock screen
    bindsym $mod+Mod1+l exec swaylock -f

    # Rofi
    bindsym $mod+d exec "rofi -show drun"
    bindsym $mod+Shift+d exec --no-startup-id "rofimoji"
    bindsym $mod+Shift+s exec --no-startup-id "rofi -show window"
    bindsym $mod+c exec --no-startup-id "rofi-cheatsheet-helper"

    # Notifications
    bindsym $mod+n exec --no-startup-id "swaync-client -t"

    # Launch applications
    bindsym $mod+F3 exec kitty ranger
    bindsym $mod+Ctrl+F3 exec pcmanfm
    bindsym $mod+Shift+F3 exec pkexec pcmanfm

    # Screenshots & Recording
    bindsym Print exec --no-startup-id $HOME/bin/screenshot-full
    bindsym $mod+Print exec --no-startup-id $HOME/bin/screenshot-window-clip
    bindsym $mod+Ctrl+Print exec --no-startup-id $HOME/bin/screenshot-region
    bindsym $mod+Shift+Print exec --no-startup-id $HOME/bin/screenshot-region-clip

    bindsym $mod+Shift+v exec --no-startup-id $HOME/bin/record-screen
    bindsym $mod+Ctrl+v exec --no-startup-id $HOME/bin/record-screen region

    bindsym $mod+Shift+i exec $HOME/bin/sway-wininfo

    # Display settings GUI
    bindsym $mod+Shift+p exec wdisplays

    # Volume controls
    bindsym XF86AudioRaiseVolume exec --no-startup-id $HOME/bin/change-volume 5%+ unmute
    bindsym XF86AudioLowerVolume exec --no-startup-id $HOME/bin/change-volume 5%- unmute
    bindsym XF86AudioMute exec --no-startup-id $HOME/bin/change-volume toggle-mute

    # Screen brightness
    bindsym XF86MonBrightnessUp exec --no-startup-id brightnessctl set 20%+
    bindsym XF86MonBrightnessDown exec --no-startup-id brightnessctl set 20%-

#
# Moving around:
#
    # Focus movement 
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    bindsym $mod+$right focus right
    
    # Move windows 
    bindsym $mod+Shift+$left exec --no-startup-id $HOME/bin/move-window left
    bindsym $mod+Shift+$down exec --no-startup-id $HOME/bin/move-window down
    bindsym $mod+Shift+$up exec --no-startup-id $HOME/bin/move-window up
    bindsym $mod+Shift+$right exec --no-startup-id $HOME/bin/move-window right
    
    # Workspace back-and-forth 
    bindsym $mod+b workspace back_and_forth
    bindsym $mod+Tab workspace next
    bindsym $mod+Shift+Tab workspace prev
    bindsym $mod+Shift+b move container to workspace back_and_forth; workspace back_and_forth
    
    # Split orientation 
    bindsym $mod+q split toggle
    
    # Fullscreen 
    bindsym $mod+f fullscreen toggle
    
    # Layout 
    bindsym $mod+s layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split
    
    # Tiling / floating toggle 
    bindsym $mod+Shift+space floating toggle
    
    # Focus mode toggle 
    bindsym $mod+space focus mode_toggle
    
    # Focus parent 
    bindsym $mod+a focus parent
    
    # Workspace next/prev 
    bindsym $mod+Ctrl+$right workspace next
    bindsym $mod+Ctrl+$left workspace prev

#
# Workspaces:
#
    # Switch to workspace
    set $ws1 1
    set $ws2 2
    set $ws3 3
    set $ws4 4
    set $ws5 5
    set $ws6 6
    set $ws7 7
    set $ws8 8
    set $ws9 9
    
    # Switch to workspace
    bindsym $mod+1 workspace $ws1
    bindsym $mod+2 workspace $ws2
    bindsym $mod+3 workspace $ws3
    bindsym $mod+4 workspace $ws4
    bindsym $mod+5 workspace $ws5
    bindsym $mod+6 workspace $ws6
    bindsym $mod+7 workspace $ws7
    bindsym $mod+8 workspace $ws8
    bindsym $mod+9 workspace $ws9
    
    # Move container to workspace
    bindsym $mod+Ctrl+1 move container to workspace $ws1
    bindsym $mod+Ctrl+2 move container to workspace $ws2
    bindsym $mod+Ctrl+3 move container to workspace $ws3
    bindsym $mod+Ctrl+4 move container to workspace $ws4
    bindsym $mod+Ctrl+5 move container to workspace $ws5
    bindsym $mod+Ctrl+6 move container to workspace $ws6
    bindsym $mod+Ctrl+7 move container to workspace $ws7
    bindsym $mod+Ctrl+8 move container to workspace $ws8
    bindsym $mod+Ctrl+9 move container to workspace $ws9
    
    # Move to workspace with focused container
    bindsym $mod+Shift+1 move container to workspace $ws1; workspace $ws1
    bindsym $mod+Shift+2 move container to workspace $ws2; workspace $ws2
    bindsym $mod+Shift+3 move container to workspace $ws3; workspace $ws3
    bindsym $mod+Shift+4 move container to workspace $ws4; workspace $ws4
    bindsym $mod+Shift+5 move container to workspace $ws5; workspace $ws5
    bindsym $mod+Shift+6 move container to workspace $ws6; workspace $ws6
    bindsym $mod+Shift+7 move container to workspace $ws7; workspace $ws7
    bindsym $mod+Shift+8 move container to workspace $ws8; workspace $ws8
    bindsym $mod+Shift+9 move container to workspace $ws9; workspace $ws9

# Border & layout defaults 
default_border pixel 0
default_floating_border normal
hide_edge_borders none

# Gaps 
gaps inner 30
gaps outer 10
smart_gaps on
smart_borders on

# SwayFX eye candy 
corner_radius 8
shadows enable
shadows_on_csd enable
blur enable
blur_xray enable
layer_effects "waybar" blur enable

# Font 
font pango:Inconsolata Nerd Font 11

# Floating 
floating_modifier $mod normal
floating_minimum_size 75 x 50
floating_maximum_size 1200 x 600

# Focus follows mouse 
focus_follows_mouse no

# Workspace back-and-forth 
workspace_auto_back_and_forth yes


# ============================================================
# ASSIGN WINDOWS TO WORKSPACES
# ============================================================
assign [app_id="spotify"] $ws5
assign [class="Spotify"] $ws5
assign [app_id="siggy"] $ws4 
assign [app_id="neomutt"] $ws4 
# Signal & Neomutt on workspace 4 
# no_focus [app_id="signal"]
# no_focus [title=".*neomutt.*"]
# for_window [app_id="signal" title="Signal"] exec --no-startup-id $HOME/bin/signal-ws4
# for_window [title=".*neomutt.*"] exec --no-startup-id $HOME/bin/neomutt-ws4
# for_window [title=".*neomutt.*"] exec --no-startup-id "sleep 0.3; swaymsg [workspace=4] layout stacking"

# ============================================================
# FOR_WINDOW RULES
# ============================================================
for_window [title="alsamixer"] floating enable, border pixel 1
for_window [app_id="mpv"] floating enable, border pixel 0
for_window [title="File Transfer*"] floating enable
for_window [app_id="(?i)galculator"] floating enable, border pixel 1
for_window [class="GParted"] floating enable, border normal
for_window [title="rofi-cheatsheet-helper"] floating enable, border none
for_window [app_id="(?i)pavucontrol"] floating enable
for_window [app_id="(?i)SimpleScan"] floating enable, border normal
for_window [app_id="delve-terminal"] floating enable, resize set 1000 600, move position center
for_window [app_id="(?i)system-config-printer"] floating enable, border normal
for_window [urgent=latest] focus

# ============================================================
# SCRATCHPAD (dropdown terminal)
# ============================================================
bindsym $mod+minus exec --no-startup-id $HOME/bin/toggle_scratchpad
for_window [title="scratchpad_dropdown"] floating enable, resize set 1200 1000, move position center, move scratchpad

# ============================================================
# MODES
# ============================================================

# Reload 
bindsym $mod+Shift+r reload

# Resize mode 
bindsym $mod+r mode "resize"
mode "resize" {
    bindsym h resize shrink width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym l resize grow width 5 px or 5 ppt

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Power menu 
bindsym $mod+0 exec --no-startup-id $HOME/bin/powermenu

# Gaps mode 
set $mode_gaps Gaps: (o) outer, (i) inner
set $mode_gaps_outer Outer Gaps: +|-|0 (local), Shift + +|-|0 (global)
set $mode_gaps_inner Inner Gaps: +|-|0 (local), Shift + +|-|0 (global)
bindsym $mod+Shift+g mode "$mode_gaps"

mode "$mode_gaps" {
    bindsym o      mode "$mode_gaps_outer"
    bindsym i      mode "$mode_gaps_inner"
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
mode "$mode_gaps_inner" {
    bindsym plus  gaps inner current plus 5
    bindsym minus gaps inner current minus 5
    bindsym 0     gaps inner current set 0

    bindsym Shift+plus  gaps inner all plus 5
    bindsym Shift+minus gaps inner all minus 5
    bindsym Shift+0     gaps inner all set 0

    bindsym Return mode "default"
    bindsym Escape mode "default"
}
mode "$mode_gaps_outer" {
    bindsym plus  gaps outer current plus 5
    bindsym minus gaps outer current minus 5
    bindsym 0     gaps outer current set 0

    bindsym Shift+plus  gaps outer all plus 5
    bindsym Shift+minus gaps outer all minus 5
    bindsym Shift+0     gaps outer all set 0

    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# ============================================================
# AUTOSTART
# ============================================================

# PolicyKit agent
exec --no-startup-id /usr/lib/mate-polkit/polkit-mate-authentication-agent-1

# XDG Desktop Portal for file pickers (auto-started via D-Bus by applications)

# Network, Bluetooth, Nextcloud tray applets (via snixembed if needed)
exec --no-startup-id nm-applet
exec --no-startup-id nextcloud

# KDE Connect
exec --no-startup-id /usr/bin/kdeconnectd
exec --no-startup-id sh -c 'sleep 2 && kdeconnect-indicator'

# Notification center
# swaync is a persistent daemon; start it once if not already running
exec --no-startup-id swaync

# Battery low warning daemon
exec_always --no-startup-id sh -c 'pkill battery-warn; /home/philipp/bin/battery-warn'

# Screen temperature (redshift replacement)
exec --no-startup-id gammastep -m wayland -c $HOME/.config/gammastep/config.ini

exec --no-startup-id kitty --class siggy siggy
exec --no-startup-id kitty --class neomutt neomutt
exec --no-startup-id sh -c 'sleep 2; swaymsg [workspace=4] layout stacking'

# Auto-tiling (uses i3ipc, works with Sway)
exec --no-startup-id autotiling -l 2

# Screen idle & lock daemon
exec_always --no-startup-id sh -c 'pkill swayidle; exec /home/philipp/bin/swayidle-daemon'

# Waybar (kill old instance before launching, same pattern as polybar)
exec_always --no-startup-id sh -c 'pkill waybar; waybar'

include /etc/sway/config.d/*
